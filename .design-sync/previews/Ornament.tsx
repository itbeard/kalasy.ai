import { Ornament } from 'kalasy-ai'

export const Red = () => (
  <div style={{ background: 'var(--night)', padding: '24px 0' }}>
    <Ornament />
  </div>
)

export const Green = () => (
  <div style={{ background: 'var(--night)', padding: '24px 0' }}>
    <Ornament green />
  </div>
)
