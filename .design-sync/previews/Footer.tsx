import { Footer } from 'kalasy-ai'

// Sections are designed for the site's dark body (background: var(--night)).
export const Default = () => (
  <div style={{ background: 'var(--night)' }}>
    <Footer />
  </div>
)
