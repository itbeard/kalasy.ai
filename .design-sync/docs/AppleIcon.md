---
category: Icons
---
Apple Podcasts icon as an unsized SVG that inherits `currentColor` — set size via `style`/`width`/`height` props (spread onto the svg). Brand color used on the site: `#B150E2`. Inside `.btn` it is auto-sized to 20px.
