---
category: Sections
---
Fixed site header with the round brand logo, the uppercase Unbounded brand line, and the БЕЛ/ENG language toggle (wired to `LangProvider`). Position is `fixed` with a top gradient that solidifies once the page scrolls 40px. Place it once per page, outside `<main>`.
