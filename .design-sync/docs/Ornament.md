---
category: Sections
---
Belarusian-embroidery-style diamond divider strip (34px tall, full width). Default is red (`var(--red)`); pass `green` for the green variant. The site alternates it between every pair of sections — use it the same way.
